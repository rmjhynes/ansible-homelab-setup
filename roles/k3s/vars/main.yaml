---
# Docker bridge gateway IP for TLS SAN (allows kubectl access from devcontainers)
k3s_docker_bridge_gateway: "172.17.0.1"

# Path to kubeconfig file
k3s_kubeconfig_path: "{{ ansible_facts['env']['HOME'] }}/.kube/config"
