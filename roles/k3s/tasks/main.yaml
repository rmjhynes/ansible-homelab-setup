- name: Download K3s install script
  ansible.builtin.get_url:
    url: https://get.k3s.io
    dest: /tmp/k3s-install.sh
    mode: '0755'

- name: Run K3s install script
  become: true
  ansible.builtin.command: sh /tmp/k3s-install.sh
  args:
    creates: /usr/local/bin/k3s

- name: Chmod kube config file for sudo-less kubectl
  become: true
  ansible.builtin.file:
    path: /etc/rancher/k3s/k3s.yaml
    mode: '0644'

- name: Verify K3s is running
  ansible.builtin.systemd:
    name: k3s
    state: started
    enabled: true

- name: Get K3s version
  ansible.builtin.command: /usr/local/bin/k3s --version
  register: k3s_version
  changed_when: false

- name: Display K3s version
  ansible.builtin.debug:
    var: k3s_version.stdout_lines

- name: Get cluster info
  ansible.builtin.command: /usr/local/bin/kubectl cluster-info
  register: cluster_info
  changed_when: false

- name: Display cluster info
  ansible.builtin.debug:
    var: cluster_info.stdout_lines
